platform :ios, '9.0'

target 'Basketball' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  # Pods for Basketball

  target 'BasketballTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BasketballUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
